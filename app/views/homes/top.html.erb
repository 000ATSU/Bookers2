<% flash.each do |key, value| %>
  <%= content_tag(:p, value, class: "notice") %>
<% end %>
<div class="container">
  <div class="row">
    <div class="mx-auto col-11 col-sm-10 col-lg-8 col-xl-6">
     <h1>welcome to <strong>Bookers</strong>!!</h1>
     <p>in <strong>Bookers</strong></p>
     <p>you can share and exchange your opinions , inpressions , and emotions</p>
     <p>about various books!</p>
     <%= link_to "Log in", user_signed_in? ? user_path(current_user.id) : new_user_session_path, class: "btn btn-info btn-sm btn-block" %>
     <%= link_to "Sign up", user_signed_in? ? user_path(current_user.id) : new_user_registration_path, class: "btn btn-success btn-sm btn-block" %>
    </div>
  </div>
</div>